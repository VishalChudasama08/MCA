# Day-2 
# 2.	Write a program to find area of a rectangle. (Area=l*b). Take input parameters from user

l = eval(input("Enter length of rectangle: "))
b = eval(input("Enter base of rectangle: "))

area = l*b;

print("Area of rectangle is ", area);