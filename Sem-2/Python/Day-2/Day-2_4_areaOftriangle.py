# Day-2
# 4.	Write a program to find area of triangle. (Area=(l*b)/2). Take input parameters from user

l = eval(input("Enter length of triangle: "))
b = eval(input("Enter base of triangle: "))

area = (l*b)/2;

print("Area of triangle is ", area);