#include<stdio.h>
#include<conio.h>
#define max 20

int main(){
	int arr[max][max], r, c, i, j;
	clrscr();

	printf("Enter rows: ");
	scnaf("%d", &r);

	printf("Enter columns: ");
	scnaf("%d", &c);

	for(i=0;i<r;i++){
		for(j=0;j<c;j++){

		}
	}
	getch();
	return 0;
}
